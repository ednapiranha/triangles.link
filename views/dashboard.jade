include mixins
extend layout

block content
  section#area
    svg#back
      filter#artifacts
        feComponentTransfer
      g(filter='url(#artifacts)')

    svg#mountain
    svg#land-upper
    svg#land-lower
    svg#mining
  #playground

  #mining-area
    img#vehicle(src='/assets/vehicle.svg')
    img.hole(src='/assets/hole.svg')

block footer
  footer
    ol#messages
    form#message(method='post')
      input(type='text', placeholder='chat here')
    span#active 0
